package com.bookorbit.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale. Reader typography is handled separately inside the WebView.
val BookOrbitTypography = Typography()
